/**
 * This should be set with the same order as when toggleling through presence status
 * UNDETERMINED -> PRESENT -> ABSENT
 */
export const enum PresenceStatus {
    UNDETERMINED,
    PRESENT,
    ABSENT,
}